from django.apps import AppConfig


class FamilyFinanceConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "family_finance"
    verbose_name = "Presupuesto familiar"

